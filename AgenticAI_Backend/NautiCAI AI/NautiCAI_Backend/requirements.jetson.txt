# Jetson / aarch64 + Python 3.8 — use ONLY on edge devices (not Windows).
#
# Install (from this folder): use bash scripts/install_jetson_backend.sh
# (JP5 aarch64 + Python 3.8 uses polars==0.19.12 + ultralytics --no-deps — see that script.)

# Core
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
python-dotenv>=1.0.0

# AI Vision (torch omitted — install Jetson wheel manually)
# Ultralytics: on JP5 aarch64 py3.8, install_jetson_backend.sh installs this package --no-deps.
ultralytics==8.4.47
onnx>=1.16.0
onnxruntime==1.16.3
opencv-python
numpy>=1.23,<2
git+https://github.com/facebookresearch/segment-anything.git

# PDF Generation
fpdf==1.7.2

requests>=2.28.0
psycopg2-binary>=2.9.0
PyJWT>=2.0.0
python-telegram-bot>=21.0
nest-asyncio==1.5.8
