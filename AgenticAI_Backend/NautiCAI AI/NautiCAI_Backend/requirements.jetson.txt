# Jetson / aarch64 + Python 3.8 — use ONLY on edge devices (not Windows).
#
# Install (from this folder):
#   export PYTHONNOUSERSITE=1
#   python -m pip install -U pip setuptools wheel
#   python -m pip install -r requirements.jetson.txt -c constraints-jetson.txt
# Then install NVIDIA PyTorch wheel separately (JetPack 5.1.1 v511, cp38).

# Core
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
python-dotenv>=1.0.0

# AI Vision (torch omitted — install Jetson wheel manually)
# Polars: use constraints-jetson.txt (wheel-capable version; Ultralytics needs polars>=0.20 only).
ultralytics==8.4.47
onnx>=1.16.0
onnxruntime==1.16.3
opencv-python
numpy>=1.23,<2
git+https://github.com/facebookresearch/segment-anything.git

# PDF Generation
fpdf==1.7.2

requests>=2.28.0
psycopg2-binary>=2.9.0
PyJWT>=2.0.0
python-telegram-bot>=21.0
nest-asyncio==1.5.8
