# Jetson pip constraints (optional `-c` file).
#
# Do NOT pin Polars here for JP5 + Python 3.8 + aarch64: wheels for polars>=0.20 use
# manylinux_2_24 tags that older Jetson pip stacks often reject, while polars==0.19.12
# ships manylinux2014_aarch64 wheels that install reliably.
#
# scripts/install_jetson_backend.sh installs Polars + Ultralytics on that platform explicitly.
