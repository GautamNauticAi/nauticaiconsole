# =============================================================================
# Jetson / L4T (e.g. Xavier, JetPack 5.1.1, Python 3.8)
# =============================================================================
# Do NOT add bare "torch" or "ultralytics" here — pip will upgrade torch to
# PyPI CPU builds (2.4.x) and break CUDA.
#
# Use: bash scripts/install_jetson_backend.sh
#   OR run the commands in that script by hand (see script comments).
# =============================================================================

fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
python-dotenv>=1.0.0

opencv-python
numpy
git+https://github.com/facebookresearch/segment-anything.git

fpdf==1.7.2
requests>=2.28.0
psycopg2-binary>=2.9.0
PyJWT>=2.0.0
python-telegram-bot>=21.0
nest-asyncio==1.5.8
