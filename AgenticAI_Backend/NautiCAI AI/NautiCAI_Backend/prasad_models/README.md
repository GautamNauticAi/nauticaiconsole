Drop Prasad model weights in this folder (or set `NAUTICAI_PRASAD_MODEL_DIR`).

Required / expected filenames:
- `hull_inspection_best.pt` (or `.engine`)
- `best (3).pt` (or `.engine`)
- `biofouling_best.pt` (or `.engine`)
- `resnet50_species_full_model.pt`

Optional nested locations also supported:
- `backend_deploy_repo/`
- `nauticai_production/`
