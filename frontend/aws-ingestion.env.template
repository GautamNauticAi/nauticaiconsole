# AWS S3 ingestion — Cloud Upload page (/upload)
# Copy these lines into frontend/.env.local (local dev) and into Vercel → Project → Settings → Environment Variables (production).
# Do NOT commit real keys. Do NOT use NEXT_PUBLIC_ for secrets.

AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_INGESTION_BUCKET=
AWS_S3_INGESTION_PREFIX=incoming
